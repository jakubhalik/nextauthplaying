export const metadata = { title: 'React docs exercises but in TSX with appropriate types', description: 'React docs exercises but in TSX with appropriate types', }
export default function RootLayout({ children }: { children: React.ReactNode }) { return <html lang="en"><body>{children}</body></html> }